'use server action'

export const signIn = async () => {
    try {

    } catch (error) {
        console.error('Error', error);   }
}

export const signup = async () => {
    try {

    } catch (error) {
        console.error('Error', error);   }
}